{
  "name": "user-service-library",
  "version": "1.0.0",
  "description": "",
  "main": "./lib/modules/user/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "publish": "npm run clean && npm run lint && npm run format && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^1.17.0",
    "rimraf": "^2.6.3",
    "tslint": "^5.16.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.4.5"
  },
  "dependencies": {
    "class-validator": "^0.9.1"
  }
}
